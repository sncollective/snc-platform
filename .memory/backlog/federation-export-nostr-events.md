---
tags: [federation]
release_binding: null
created: 2026-04-20
---

# Nostr Event Export

Export a user's Nostr events as signed JSON (the standard portable Nostr format). Exported events are valid for import into any Nostr relay, preserving the cryptographic signatures created at publish time.
