# Session — ARD v0.1→v0.4.1 upgrade + email research engagements

**Date:** 2026-06-08
**Scope:** platform `.claude/`/`scripts/`/`.memory/` (ARD upgrade) + platform `.research/`/`.work/` (email-capture engagement); engagement #2 (email infra) is **root**-scoped and deferred.

## Arc A — ARD upgrade v0.1 → v0.4.1 (done, merged to main, pushed)

Upgraded platform's vendored ARD adoption to **v0.4.1** via **Path B** — adopt the in-tree `kernel/` consumption contract rather than re-vendoring prose. Decomposed into 5 children, all closed:

- **platform-0014** decision record — adopt the kernel contract at v0.4.1, in-tree; revises platform-0013's consumption mechanism + version pin.
- **SSRF lint patch** — hardened `lint-citations.py`'s `url_alive()` (scheme allow-list http/https; public-IP-only resolution refusing loopback/link-local/private/reserved/multicast/unspecified — closes the `169.254.169.254` metadata-endpoint hole; per-redirect re-validation).
- **Vendor `ard-kernel/`** — full ARD v0.4.1 kernel surface vendored byte-faithful at top-level `ard-kernel/` (23 files + LICENSE). (Platform is self-contained — it vendors rather than referencing root's `ard/` submodule.)
- **4a — `lint-citations.py` → shim** — replaced the ~340-line fork with a 24-line `runpy` shim delegating to `ard-kernel/lint-citations.py` (the vendored reference). Output byte-identical. Per platform-0014, consume the citation lint as the vendored verbatim reference, not a fork.
- **4b — `lint-research-claims.py` data-sourced** — renamed 2 `PATTERN_SPECS` keys to canonical; added `load_catalog_config()` reading `ard-kernel/catalogs.json` (`--catalogs` override, built-in fallback). SNC-tuned regex matchers + severities stay local. Behavior-preserving.
- **5 — slim+bump the 3 rule files** — `research-band-{spec,catalogs,platform}.md` bumped to v0.4.1; spec added §10.5 (typed cross-references); catalogs §1 slimmed to a 9-shape fence-map + defer-to-data, §3 colliding-handle, §9 predicates, §2 IP-profile note.

New tests: `scripts/tests/test_lint_citations_url_alive.py` (11 cases, pointed at the vendored reference) + `test_lint_research_claims_catalog.py` (5 cases).

**State:** 6 commits, fast-forwarded to `main`, **pushed**. `main == origin/main`.

> Note: the platform-root `AGENTS.md`/`CLAUDE.md` (outside this worktree, on `main`) still say "ARD v0.1" in the research-band section — the in-worktree `CLAUDE.md` was updated to describe the vendored-kernel lints. A doc-sync pass to bump those two prose labels to v0.4.1 is an open tidy (low priority; the rules + decision record are authoritative).

## Arc B — research engagements

### #1 Email capture at shows (done, committed `b193f1a`, on this branch)

Focused single-pass research engagement (ARD standard rigor): capturing audience emails at live shows (sign-up sheet + QR) → land as platform contacts → funnel into the band's own recurring revenue (Stripe subscribe / patronage / donate). The **funnel hook is the headline value**, per the user's "bonus points" steer.

- **Output:** `.research/analysis/briefs/email-capture-at-shows-landscape.md` + 5 attestations (3 source-direct: Tally webhooks, Laylo, iubenda/GDPR; 2 honestly-marked search-summaries).
- **Verified:** lint 0 broken chains; adversarial-read caught 1 overclaim ("native is the *only* way to unify capture+subscribe" — false; hosted-form-webhook also lands data in-platform) → fixed at spot-check. The real native advantage is owning the **capture surface** (at-capture UX + consent-at-capture + direct DB write), not data-unification.
- **Recommendation → build native** (the conversion goal is platform-owned recurring revenue; a bought fan-CRM funnels into its own rails with no Stripe/subscription hook).
- **Scoped:** `email-capture-at-shows` feature `[community, commerce]`, stage `drafting`, ready for `/design`. Carries build-vs-buy resolution, 4 likely stories, 5 open questions, risks, connections to 3 backlog items.

### #2 Email infrastructure ARD refresh (root-scoped, NOT started — deferred to a fresh session)

Evaluate whether to continue Proton Business + add co-founder seats or migrate — covering **both** the operational mailboxes (collaboration/promotion/band management) **and** the outbound transactional sender (currently Proton relay). User preference on record: **prefer self-hosting, open source, avoiding vendor lock-in.** Chosen as **option "b"** — an **ARD refresh** of the existing pre-ARD root brief (`/home/agent/SNC/.research/analysis/briefs/email-infrastructure.md`, dated 2026-03-23), re-authored under ARD discipline (attest sources, ground the unsourced pricing/deliverability figures, citation chains, verification stack), **refreshed in place** to keep the parked item's inbound reference intact.

**Placement resolved → root, not platform/org** (the user's "does it really belong in root or platform?"):
- *Not platform* — email infra is bounded by **org-owned shared resources** (the `s-nc.org` domain, its DNS, team mailboxes). Platform only *consumes* the transactional sender via a config pointer (`SMTP_HOST` → `apps/api/src/email/send.ts` Nodemailer); it doesn't own the infra. The mailbox facet has zero platform claim.
- *Not `org/`* — `org/` has `.memory/`+`.research/` but **no `.work/` band**, so the execution item (the parked Migadu-trial work item) can't live there; splitting brief-in-org/work-at-root would fracture it.
- *Root* — genuinely cross-cutting (org mailboxes + platform consumption, linked by shared domain/provider); the "same provider or split?" question needs both facets in one view; root has all three bands and already holds both artifacts.
- **The one platform-scoped sliver:** the *integration verification* — does the chosen sender's SMTP work with platform's Nodemailer setup — a small platform task **downstream** of the root decision, not the decision itself.

The placement reasoning + ARD-refresh next-step are also annotated on the root parked item `email-infra-evaluate-migadu.md` so a fresh session lands on it.

## Open questions / next steps (for a fresh session)

- **Engagement #2 (email infra ARD refresh)** — run as a **root** engagement. The prior brief has anchor-and-drift exposure: unsourced figures `~$9/mo` (Migadu), SES `$0.10/1000`, Proton `~$4–8/mo per seat`, Namecheap `~$1–4/mo` — ground or hold each. Honor the self-host/open-source/anti-lock-in preference (Stalwart AGPL / Mox MIT were noted as future options; re-weigh given the preference + a possible Proxmox self-host path). Refresh in place. This is a **cross-repo** op from this worktree (write root `.research/`, commit root repo) — cleaner to run from a root-rooted session.
- **`/design` the `email-capture-at-shows` feature** — drafting → design; firm + split the 4 stories; the lead open question is at-show offline tolerance (native PWA/offline-queue vs hosted-form robustness — test, don't dismiss).
- **Doc-sync tidy** — bump the "ARD v0.1" prose labels in platform-root `AGENTS.md`/`CLAUDE.md` to v0.4.1 (low priority).

## State

- **Arc A:** on `main`, pushed. Clean.
- **Arc B #1 + this session note:** on branch `worktree-bridge-cse_01KacUb3msYTzps7U8N6aXHa`, ready to fast-forward to `main` + push.
- **Arc B #2:** captured (parked-item annotation + this note); not started.
</content>
</invoke>
