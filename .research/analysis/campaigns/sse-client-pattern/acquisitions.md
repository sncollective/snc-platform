---
campaign: sse-client-pattern
updated: 2026-06-15
---

# Acquisition manifest — sse-client-pattern

Consolidated acquisition candidates from the three specialists. Both enriching,
non-blocking — no held claim depends on them, so the position stands without them. Promotion
into `.work/` (the `research-acquisition-queue`) is operator-confirmed; not auto-written.

| source | class | web-availability | urgency | completes |
|---|---|---|---|---|
| A maintained fetch-based SSE client (fresher than `@microsoft/fetch-event-source`, last publish 2021) | ingestible | online (comparison page not yet fetched) | enriching | Only the transport native-vs-fetch decision IF native is ever reversed (cross-origin / POST / bearer-token futures) — see position `revisit_if`. Inert otherwise. |
| MDN "Using server-sent events" full guide | portal | online (fetched; reconnection/`id:`/204 prose was outside the returned excerpt) | enriching | A developer-facing restatement of the reconnection model; the WHATWG spec already covered it normatively, so non-load-bearing. |

Neither blocks the position or any consumer. No `blocking` candidates surfaced — every
load-bearing source was fetched and attested this engagement.
