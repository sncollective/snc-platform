<!-- ARD-Version: 0.4.1 -->
# Per-corpus INDEX — template

A numbered bibliography of one corpus (ARD SPEC §10.2, reference tier). Lives at `.research/reference/<corpus>/INDEX.md`. The entry number `N` is the anchoring target for `[handle]{N}` citations — **append new entries; never renumber** (renumbering breaks every existing citation).

> **Source license:** <upstream license for this corpus>.

## Tag vocabulary

The active theme tags for this corpus (coined freely on ingest, lightly consolidated). `overview` is the one cross-corpus stable tag (survey/meta-shape pieces).

## Entries

### 1. <Title> — `<citation-handle>`

- **Source class:** <paper | book-chapter | blog-post | standard | ...>
- **Author:** <Name>
- **Source URL:** <url>
- **Original date:** <date>
- **Ingested:** <YYYY-MM-DD>
- **Raw fetch:** `<slug>.raw.<ext>` (gitignored) — or `none locally`
- **Themes:** <tag, tag, ...>
- **Covers:** <one-line audience-neutral description, ~120–180 chars>

### 2. <Title> — `<citation-handle>`

- ...
